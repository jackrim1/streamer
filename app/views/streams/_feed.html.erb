<div class="column is-half">
	<% if user_signed_in? %>
	<article class="media box">
		<figure class="media-left">
			<p class="image is-64x64">
				<img src="https://bulma.io/images/placeholders/64x64.png">
			</p>
		</figure>
		<div class="media-content">
			<%= render 'form' %>
		</div>
	</article>
	<% end %>
	


	<% @streams.each do |stream| %>
	<div class="box">
		<article class="media">
			<div class="media-left">
				<figure class="image is-64x64">
					<img src="https://bulma.io/images/placeholders/64x64.png">
				</figure>
			</div>
			<div class="media-content">
				<div class="content">
					<strong><%= stream.user.name %></strong>
					<small>(<%= stream.user.job_title%>)</small></br>
					<small><%= stream.user.department%></small></br>
					<p><%= stream.stream %></p>
				</div>

				<% if stream.user == current_user %>
				<nav class="level">
					<div class="level-left is-mobile">
						<%= link_to stream, class: "level-item" do %>
							<span class="icon"><i class="fa fa-link" aria-hidden="true"></i></span>
						<% end %>
						<%= link_to edit_stream_path(stream), class: "level-item" do %>
							<span class="icon"><i class="fa fa-pencil" aria-hidden="true"></i></span>
						<% end %>
						<%= link_to stream, method: :delete, data: { confirm: "Are you sure you want to delete this Stream?"} do %>
							<span class="icon"><i class="fa fa-trash-o" aria-hidden="true"></i></span>
						<% end %>
					</div>
				</nav>
				<% end %>
			</div>
		
		</article>
		</div>
	<% end %>

</div>














